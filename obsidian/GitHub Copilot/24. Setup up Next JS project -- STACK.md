## Stack - Local Agents
Next JS (fullstack fwk)
Shadcn ui + tailwind css
Postgress DB on Neon
Drizzle ORM  (para interactuar con la bbdd)
Clerk for auth

Zod -> backend

## Requisitos

Next JS


## Instalación GitHub Copilot CLI (no necesario)

Nos situamos en el workspace correcto (ojo con la carpeta en que abrimos)
winget install GitHub.Copilot  --> esto creo que no me fue bien
 winget install --id GitHub.cli

apliAA\calendarproject> copilot
Cannot find GitHub Copilot CLI (https://docs.github.com/en/copilot/how-tos/set-up/install-copilot-cli)
Install GitHub Copilot CLI? (y/N): y


## 24. Setup up Next JS project

Ejemplo https://github.com/tomphill/linkshortenerproject 

Next.js as our full-stack framwork   (también Next.js backend)
shadcn/ui for pre-built UI components
Tailwind CSS for styling
PostgreSQL Database hosted on Neon
Drizzle ORM for database interactions
Clerk for Authentication
Zod for validation

#### Crear el proyecto Next.js
1. En una terminal de VS Code:    
$ npx create-next-app@latest calendarionoroeste

Yes, seleccionar opciones por defecto....
cd calendarionoroeste

2. Abrimos una terminal para instalar shadcn en este proyecto. Vamos a la docu https://ui.shadcn.com/docs/installation, seleccionamos Next.js , https://ui.shadcn.com/docs/installation/next
	- Shadzen UI
	$npx shadcn@latest init
	    Elegimos todo por defecto para este proyecto...
#### Configurar autenticación con Clerk 
2. Ir a clerk.com (autenticación)
- Create Application. Name CalendarioNoroeste
- Opción Sign In solo mail (desmarcar Google)
- Seleccionamos Next.js, y copiamos prompt. Lo pegamos en el chat de GitHub Copilot
	- Install Clerk CLI   ($ npm install -g clerk)
	- Finalmente creará un fichero proxy.ts (antiguamente middleware.ts), un fichero env.local, etc.

Copio aquí esquema del prompt....
1. Install or update the Clerk CLI
2. Set up Clerk in this project, or scaffold a new app if it's empty
3. Verify the Next.js proxy matcher when applicable
4. Start your app with Clerk installed.


#### Crear bbdd Postgress y configurar el ORM

1. Lo creamos desde la web de postgress, neon.com
	1. Nombre del proeycto
	2. AWS Europe West (London)
	3. La llamamos calendarionoroestedb
2. Lo siguiente que es set up e instalar Drizzle ORM en nuestro proyecto. 
	1. Ir a  https://orm.drizzle.team   para isntalar sus dependencias. Botón "Get started"
	2. Link Get Started, y seleccionar NEON https://orm.drizzle.team/docs/get-started/neon-new
	3. Ir a las isntrucciones de instalación y ejecutar en una terminal
			npm i drizzle-orm@rc @neondatabase/serverless dotenv
			npm i -D drizzle-kit@rc tsx    -> instalar estas dependencias
		3. En el Step 2 de las instrucciones de la docu, manda "Setup connection variables" Create a `.env` file in the root of your project and add your database connection variable (renombraremos .env.local que ya existe a .env):
			```
			DATABASE_URL=
			```
		4.   Step 3 - Connect Drizzle ORM to the database
			Create a `index.ts` file in the `src` directory (a lo mejor hay que crearlo) and initialize the connection:
3. 
			import { drizzle } from 'drizzle-orm/neon-http';
			const db = drizzle(process.env.DATABASE_URL);  -> a lo mejor hay que añadir exclamación  ! al final si da error por no conocer el valor 
		5. Deberíamos ir al setp 4 - Create table, pero lo dejamos para adelante. En la docu hay un ejemplo, pero yo pediré al chat que me haga los esquemas de las tablas necesarias para el modelo de la aplicación. No usaremos src, usaremos una carpeta db, que contendrá schema.ts (y más ficheros de los próximos pasos)
		6. Step 5 - Setup Drizzle config file
			5. Crear el fichero drizzle.config.ts en la raíz del proyecto. el contenido lo copiamos de la docu. 
				1. Tenemos que cambiar el file del schema. Cambiamos schema: './src/db/schema.ts', y ponemos el nuestro que será: ".db/schema.ts"  (no dejaremos nuestro esquema en  src)
				2. chat de github copilot creará el esquema por nosotros 
			6. Creamos el fichero schema.ts dentro de la folder db
			7. Volvemos a la documentación de clerk
				1. Ya habíamos lanzado el prompt y el fichero .env se había cubierto automaticamente. AHí en cualquier caso tenemos los valores
				2. Set your clerk apik en .env
			8. Ejecutar npm run dev -> https://localhost:3000

#### Custom agents
Fichero AGENTS.md (está creado  automáticamente quizás por algún prompt de Cloude que  ya ejecutamos)

El siguiente prompt generará contenido en el AGENTS.md

[Agent]: 
Build out the content for this agent instructions file for this project. This is an instructions file specifically for LLM's to adhere to the config standars for this project. Agent instructions will be separated out into separate .md markdown files located in the /docs directory.  

- Borramos los .md bajo docs que generó el prompt
- Borramos las referencias a esos .md en el AGENTS.md
- Indicamos lo siguiente (ojo  a las palabras en mayúsculas...)

## ⚠️ CRITICAL: Required Reading Before Code Generation
**🚨 MANDATORY REQUIREMENT 🚨**
Before generating ANY code, you MUST:
1. **READ the relevant documentation files** in the `/docs` directory that apply to your task
2. **FOLLOW the patterns and guidelines** specified in those files
3. **DO NOT proceed** with code generation until you have reviewed the applicable documentation


Seleccionar en la ventana de chat la opción Agent Customizations 
Agents > New Agent > "Select a directory for the new customization file in ..."
Seleccionamos .github\agents y le damos el nombre al fichero instructions-generator, y él lo llamará **instructions-generator.agent.md**
El fichero se crea dentro de .github\agents con la siguiente configuración y contenido:

---
name: Instructions Generator
description: "This agent generates highly specific agent instruction files for the /docs directory"
tools: ["read", "edit", "search", "web"]

-- -
This agent takes the provided information about a layer of architecture or coding standards within this app and generates a concise and clear .md instructions file in markdown format for the /docs directory.


#### Prompt file (custom)
1. Creamos un prompt que llamamos **create-instructions.prompt.md**
---
agent: Instructions Generator
-
Take the information below and generate an agent instructions .md file for it in the /docs directory. If a .md filename is provided, use that, otherwise generate an appropriate filename based on the generated content. Make sure the instructions are concise and not too long. Make sure to update the AGENTS.md file to reference this new docs file. If no information is provided below, prompt the user to give the necessary details about the layer of architecture or coding standards to document. 


2. Lo invocamos en el chat con **/create-instructions:**  seleccionando modo Agent

Qué cosas hará?
- hará un switch (porque está en modo Agent, sino, si es plan o ask,  no tendria 'privilegios') al modo agent: Instructions Generator, para ejecutar nuestro prompt
- Luego generará el contenido para nuestros nuevos ficheros de instrucciones,creará referencias en el agents.md, etc

A continuación, en el chat usaremos el /create-instructions seguida de toda la información que queremos incluir en nuestro primer fichero de instrucciones.
- el primero será el de autenticación. La aplicación usa clerk por tanto hay que especificar que queremos routes, protegidas, que necesitan que el usuario esté logueado antes de acceder a esa ruta
- Por ahora solo 2 paginas 
	- home    /      -> si el usuario está logueado, la home debe redirigir  a /calendario
	- calendario /calendario

- La autenticación en la aplicación estará administrada por clerk. Poner en letras mayusculas que no se usarán otros métodos de autenticación
- También diremos que el sign in y el sign up via clerk debe hacerse lanzando una pantalla modal


[Agent]:
/create-instructions everything to do with auth in this app is handled by clerk. NO OTHER AUTH METHODS SHOULD BE USED. The /dashboard page is a protected route and must require the user to be logged in to be able to access this page. If the user is logged in and trying to access the homepage, they should be redirected to the /dasboard page. Sign in and sign up via clerk should always launch as a modal.

3. Generamos nuevo fichero bajo .github/prompts
AGENTS.md -> este file en el contexto
 /create-instructions ALL UI elements in this app use shadcn ui components.
 
 DO NOT create any custom components,
 ALWAYS use shadcn ui components.
 Model:Instructions Generator Agent (custom) 

4. selección Agent
Create a /dashboard page. DO NOT add any content or data fetching just yet, just add a Dashboard h1 tag as aa placeholder

5. contexto page.tsx (en el component Dashboard) 
make sure this page adheres to the auth coding standards

Vamos a la configuración del proyecto Calendar en Clerk, Configuration
	EMAIL
	Deshabilitamos Verify at sign-up
	Deshabilitamos Email verification code
	PASSWORD
	 Client trust -> disabled
La autenticación ahora funciona
Ahora lanzamos este prompt:

6. make sure this page adheres to the auth coding standards #file:authentication.md
Agent

`app/page.tsx` actualmente no aplica autenticación. Según `authentication.md`, la página principal debe redirigir a `/calendar` cuando el usuario ya está autenticado.

Ahora --> La página seguirá siendo pública para usuarios no autenticados, mientras que los usuarios autenticados serán enviados al calendario.

7. Nuevo prompt relacionado con estilos

make sure the entire app displays as dark mode

CTROL+SHIFT +P  -->  >simple Browser integrated
8. Nuevo prompt 
make sure this page adheres to the auth coding standards

Además, en el navegador integrado en VS Code, seleccionamos un control, botón derrecho y seleccionar "Add element to chat"

9. Nuevo prompt
Update the agents.md file to make sure that it's incredibly important to ALWAYS read the relevant individual instructions files within the /docs directory BEFORE generating ANY code